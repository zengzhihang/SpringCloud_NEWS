spring:
  cloud:
    config:
      name: comment,eureka,feign,database,redis,key
      label: master
      discovery:
        enabled: true
        service-id: config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/

